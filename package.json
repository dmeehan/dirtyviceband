{
  "name": "dirtyviceband.com",
  "version": "0.1.0",
  "scripts": {
    "build": "sass scss/app.scss css/app.css --style=expanded --no-source-map",
    "watch": "sass scss/app.scss css/app.css --style=expanded --no-source-map --watch"
  },
  "devDependencies": {
    "sass": "^1.101.0"
  }
}
